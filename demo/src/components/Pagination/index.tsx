import { Pagination } from '@nan-design/react';

const PaginationCom = () => {
    return (
        <>
            <Pagination total={60}></Pagination>
        </>
    );
};

export default PaginationCom;
